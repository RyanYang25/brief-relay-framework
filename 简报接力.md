# 简报接力框架 (BRIEF Relay Framework, BRF) v1.5

> **方法定位（一句话）**：用一份持续演进的「项目简报（BRIEF）」作为唯一入口，配合「契约头 + 接力棒 + 恢复协议」三件套，让任何 AI Agent 平台都能零成本接手、跨平台可迁移、项目上下文不丢失。
>
> **名称由来**：「简报」= 核心 artifact（BRIEF.md）；「接力」= 跨 Agent / 跨平台 / 跨会话的上下文传递逻辑——谁产、谁消费、下一步动谁，显式声明，像接力棒一样一棒交一棒。
>
> **适用范围**：咨询、写作、研发、运营等一切需要多文件协作与状态跟踪的项目。领域无关，不绑定任何 IDE 或 Agent 平台。
>
> **版本说明**：本文件为 v1.5 版。v1.0 为首次正式命名发布版；v1.1 新增「十二、工具与脚本依赖的跨电脑可迁移性规范」，源自实际跨电脑部署（NAS / 网盘默认排除隐藏文件夹）的踩坑沉淀；v1.2 新增「十三、项目自描述增强：身份速览块 + 接手自检清单」，吸收标准化 skill 的元信息自描述与打包前实测思想（不套外壳、取其神），提升任意项目被新 Agent 接手时的「一眼可懂、接手有章」；v1.3 新增「十四、留痕与决策生命周期（四态模型）」，将 Open/Decisions/Resolved/Archive 四态生命周期显式化（Open 态补强、Decisions 增负责人/影响范围、Archive 显式化退役≠删除），并明确原始会话证据与结构化文档的分工；v1.4 新增「十五、四态巡检清单与活跃上下文轻量化」，将 v1.3 四态模型操作化为迭代收尾必做的「四态巡检清单」（Open→Decisions→Resolved→Archive 逐项自检、四态均闭合方算完成），并反哺「活跃上下文轻量化」纪律；v1.5 新增「十六、四态就绪门禁与平台入口适配器」，把 v1.4 的"人工巡检"再推半步——A 将四态巡检清单升级为**机器可验证的交接就绪门禁**（轻量脚本自动核对四态闭合度并出报告），B 提供 **BRIEF⇄AGENTS.md/CLAUDE.md 双向映射**的纯 Markdown 平台入口适配器，让 BRF 项目既能守住"单一入口权威"，又能吃上主流编码平台（Cursor / Claude Code / Roo 等）的自动接手红利，二者均不引入外部依赖、不违反去平台绑定红线。

---

## 核心理念

### 一、BRIEF 是「入口 + 演进引擎」，不是静态门牌

BRIEF.md 同时承担两个角色：

1. **接入点**——任何新 Agent / 新会话 / 换平台时，只读这一个文件即可上手；
2. **演进引擎**——项目推进中，阶段性自动触发 + 用户手动要求时，持续更新 BRIEF 及其联动文件，让上下文随项目一起生长。

> **为什么不降级成 AGENTS.md**：AGENTS.md 是编程 Agent 圈的一个"项目级静态入口"约定——平台在新会话开始时读一次，告诉你"项目长这样"，之后它就沉默了，不具备自我演进能力。BRIEF 的本质是活的：它既管接手，也管演进。把 BRIEF 降格成 AGENTS.md，等于把一个活系统塞回静态门牌，是降维。本框架不依赖 AGENTS.md，BRIEF 自包含全部接手与演进信息。

### 二、可迁移性是第一约束

整套机制只用纯 Markdown + HTML 注释实现，**不依赖任何平台的特有能力**（无 YAML frontmatter 依赖、无平台私有记忆依赖、无"中断回调"依赖）。判定标准：把整个项目文件夹拷到另一个 Agent 平台（WorkBuddy / Trae / Qoderwork 等），新 Agent 读 BRIEF 顶部的契约头即可接手——这是本框架的硬指标。

---

## 提示词正文（通用搭建模板）

> 复制以下内容发给任意 Agent，替换 `{项目名称}`、`{项目描述}` 及可选占位符即可快速搭建项目基座。
> 适用于咨询、写作、研发、运营等各类需要多文件协作与状态跟踪的项目。

---

> **元指令（Guardrail）**：生成的 BRIEF.md 顶部必须包含一条防御性注释，防止 Agent 或后续协作者只修改 BRIEF.md 而忽略联动文件。参考格式：
>
> ```
> > **📌 如果你收到"更新 brief / 更新项目状态 / 同步进度"类指令**：
> > 不要只改本文件！先读本文件的「更新 SOP」章节，找到对应场景的操作指引，
> > 按 SOP 顺序联动更新 current.md / standards.md / PROJECT_GUIDANCE.md 等文件。
> ```

你是一个项目管理助手。请为项目 **{项目名称}** 搭建 project-context 工作目录。

**项目背景：** {项目描述，2-3句话说明目标、交付物、关键约束}

### 一、目录结构

```
{项目根目录}/
├── BRIEF.md                  ← 项目简报（唯一入口 + 演进引擎，放在根目录，含 @context-root 契约头）
├── {会议记录目录}/            ← 会议/沟通记录（默认 B02 会议记录/，可自定义，见下方编码说明）
├── project-context/
│   ├── context/
│   │   ├── current.md        ← 当前状态与进行中任务（更新最频繁，含 @context 契约头）
│   │   └── standards.md      ← 工作标准与输出规范（只增不改，含 @context 契约头）
│   ├── references/           ← 原始材料（数据、文档、参考资料）
│   ├── handoffs/             ← 历史产出（已完成的交付物）
│   ├── .working/             ← Agent 临时文件（自动清理，不纳入版本管理）
│   ├── tools/                ← 项目自带工具/脚本（纯文件、全平台可读，随项目携带）
│   ├── changelog.md          ← 结构变更日志（仅记录文件增删、目录调整）
│   └── PROJECT_GUIDANCE.md   ← （可选）详细百科全书，记录历史决策与完整任务清单
```

**目录编码（用户个人习惯，Agent 须识别并匹配，不得强制推荐）**：用户通常用手写字母前缀对根目录文件夹分类（如 `A01 中间文档/`、`B02 会议记录/`、`C01 同事成果/`、`C02 参考资料/`）。这套编码是用户手动建立的，Agent **不应发明或推荐新编码方案**，而是：

- **首次接触项目时**：扫描根目录，识别已有的编码前缀与含义（如 `A`=过程/`B`=沟通/`C`=参考）；
- **新建或调整文件夹时**：沿用用户既有的前缀规则与编号习惯，保持连续（如新增参考类目录用 `C03 …`）；
- **在 BRIEF.md 的「文件组织」中**：如实标注用户采用的编码含义，不另行规定。

> **注意**：会议记录**不放在** project-context/ 内。建议在项目根目录下建立独立的 `{会议记录目录}/`（默认 `B02 会议记录/`），通过 BRIEF.md 的文件索引建立链接。会议记录体量大、更新频率高，独立存放避免污染 project-context 结构。

### 二、契约头规范（@context）—— 接力的握把

每个 context 文件顶部放置一个 `<!-- @context -->` HTML 注释块，声明该文件的角色与接力关系。这是「接力棒」的握把：新 Agent 一摸就知道这文件是干嘛的、什么时候该看、什么时候该改、改完下一步动谁。

**设计决策（为何用 HTML 注释而非 YAML frontmatter）**：

- YAML frontmatter 需要平台有 parser 才认；HTML 注释是任何"读文件文本"的 Agent 都能看到的通用格式，最符合「跨平台、纯 Markdown 即可接手」目标；
- HTML 注释**不渲染、不污染显示**——人类打开文件看不到这块，只有 Agent 读时才存在。

**必填字段**：

| 字段 | 含义 |
|------|------|
| `role` | 本文件在项目中的角色（如"唯一入口""活跃上下文""约束层"） |
| `produced_by` | 谁维护本文件（如"更新 SOP 自动维护""用户手动"） |
| `consumed_by` | 谁该读本文件（如"所有 Agent 每次会话开头必读"） |
| `update_trigger` | 什么条件下必须更新（如"任何状态/决策变更后"） |
| `upstream` | 上游依赖（本文件依赖谁的信息，如 BRIEF.md 依赖 current.md 的状态） |
| `downstream` | **接力棒指向**——本文件更新后，下一个该动谁（如 current.md → B01 直播课程记录/、standards.md） |

**BRIEF.md（@context-root）示例**：

```
<!-- @context-root
role: 项目唯一入口 + 演进引擎
produced_by: 更新 SOP（自动/手动触发）
consumed_by: 所有 Agent 与人类接手者，每次会话开头必读
update_trigger: 任何状态/决策/文件结构变更后
upstream: context/current.md（活跃状态）、context/standards.md（约束）
downstream: context/current.md → context/standards.md → {会议记录目录}/
-->
```

**context/current.md（@context）示例**：

```
<!-- @context
role: 当前活跃上下文（activeContext）
produced_by: 更新 SOP 场景二/三自动维护
consumed_by: 所有 Agent 每次会话开头必读
update_trigger: 任务状态变更 / 新增决策 / 发现阻塞点后
upstream: BRIEF.md（范围与约束）、standards.md（怎么做）
downstream: {会议记录目录}/、standards.md（踩坑沉淀）、PROJECT_GUIDANCE.md（重要决策）
-->
```

**context/standards.md（@context）示例**：

```
<!-- @context
role: 约束层（输出规范 / 协作流程 / 历史踩坑）
produced_by: 更新 SOP 场景五（只增不改）
consumed_by: 所有 Agent 产出前必读
update_trigger: 新增规范或踩坑记录时
upstream: BRIEF.md（项目画像与约束）
downstream: {会议记录目录}/_模板.md、references/、handoffs/
-->
```

> **接力有效性验证**：可用"全新子 Agent（不带历史记忆、禁读平台记忆目录）仅依 BRIEF + @context 块接手"的方式实测——能通过即说明项目已自描述、可迁移达标。

### 三、搭建要求

**BRIEF.md（项目简报 = 入口 + 演进引擎）**

这是整个目录的唯一入口，也是随项目演进的核心文件。任何 Agent 或人类第一次接触项目时只读这一个文件。内容控制在一页以内，包含以下板块：

1. **元指令（Guardrail）**
   
   - 在文件顶部（标题下方）插入一条防御性注释（格式见上方元指令）
   - 目的：防止 Agent 做表面更新而遗漏联动文件（如 current.md、PROJECT_GUIDANCE.md）
   - 同时标注：最后更新日期、更新人、更新原因

2. **项目画像**（稳定信息，几乎不变）
   
   - 项目名称、目标、交付物、关键约束、时间节点
   - 利益相关方：使用 ⭐ 标记决策权等级的表格格式：
     
     ```
     | 角色 | 姓名 | 决策权 |
     |------|------|--------|
     | 项目负责人 | 某某 | ⭐⭐⭐ 最高 |
     | 对接人 | 某某 | ⭐⭐ 高 |
     | 执行/顾问 | 某某 | ⭐ 中 |
     ```

3. **文件组织**（实际目录树）
   
   - 展示项目根目录的实际文件结构（不仅是 project-context/）
   - 用缩进树形展示，标注用户实际采用的编码含义（Agent 须先识别既有规则，不得套用模板预设）
   - 示例：`├── A01 中间文档/`、`├── B02 会议记录/`、`├── C01 参考资料/`（以用户实际为准）

4. **当前状态**（一句话 + 指针）
   
   - 用一句话概括当前进度，例如"第二版方案已完成，待客户反馈"
   - 指向 `context/current.md` 获取详情

5. **文件索引**（路由表）
   
   - 列出 context/、references/、handoffs/、{会议记录目录}/ 下所有文件，每个文件一行说明用途
   - 当文件增删时同步更新此索引

6. **读取规则**（渐进式披露 + 契约头说明）
   
   - 标注哪些文件必须先读（如 current.md），哪些按需读（如 references/ 下的具体文件）
   - 说明每个文件顶部 `<!-- @context -->` 块的作用，告诉接手者优先读这些块即可 30 秒掌握"项目怎么组织、每次该动哪个文件"
   - 如有 PROJECT_GUIDANCE.md，标注为"按需查阅"的第四层
   - **会话恢复协议**（见第九章）：接手可靠性不依赖"中断时自动同步"，而靠恢复协议 + 增量落盘两条规则

> **长度触发**：当 BRIEF.md 超过一页（约 80 行）时，将稳定信息之外的细节迁移到 PROJECT_GUIDANCE.md，保持入口精简。

**context/current.md（当前状态）**

记录活跃信息，更新频率最高：

- 进行中的任务列表（每条含：任务名、**状态（见下方状态枚举）**、下一步动作、阻塞点、责任人）
- 近期决策记录（**决策内容 + 依据/理由 + 日期**——"依据/理由"字段至关重要：接手者不只看结论，更要看为什么这样定，避免误改或重复讨论）
- 待解决问题
- 会议纪要索引（**只存"日期+主题+≤5条核心要点+文件路径"，不堆砌全文或长段落**）

> **长度触发**：当 current.md 超过 {N} 行（建议 120 行）时，将历史任务清单、过期决策、会议全文细节迁移到 PROJECT_GUIDANCE.md，current.md 只保留活跃信息。

**任务状态枚举（统一使用）**：
`未开始` → `进行中` → `已完成`；异常态：`已阻塞`（须注明原因）、`已取消`（须注明原因）

**context/standards.md（工作标准）**

记录输出规范和工作流程，只增不改：

- 文档格式要求（如风格、模板、命名规范）
- 质量标准（如验收条件、review要点）
- 协作流程（如角色分工、审批节点）
- 历史踩坑记录（做错过什么、怎么修正的）

**references/（原始材料）**

存放项目输入：数据文件、背景文档、客户资料、竞品分析等。BRIEF.md 的文件索引中为每个文件写一行说明。

**handoffs/（历史产出）**

存放已完成的交付物。按版本或日期命名，如 `v1-初稿.md`、`20260612-方案汇报.pptx`。当前版本不要放这里，放在项目根目录或按你习惯的位置。

**PROJECT_GUIDANCE.md（可选 —— 详细百科全书）**

当项目发展到一定规模、current.md 变得过长时启用。定位是"历史决策完整记录"，供按需查阅，不作为日常入口。

- 记录内容：完整时间节点、历史决策详情（时间+来源+内容+**理由**）、完整任务清单（含已完成和已取消）
- 与 changelog.md 的分工：
  - `changelog.md`：只记录 **文件层面的结构变更**（如"新增XX文件""删除YY目录"），供维护者看
  - `PROJECT_GUIDANCE.md`：记录 **项目层面的业务变更**（如"党组会决定XX""需求范围调整为YY"），供所有人看
- 如不需要（项目规模小），可以不加这个文件，用 current.md + changelog.md 即可

**changelog.md（变更日志）**

记录 **文件层面的结构变更**，格式：

```
## YYYY-MM-DD
- 变更内容（新增/修改/删除了哪个文件，原因）
```

> **区分**：项目业务变更（如"党组会决定XX"）记录到 `PROJECT_GUIDANCE.md` 或 `current.md`，不记入 changelog.md。changelog.md 只给维护者和 Agent 看，用于理解文件结构的演变。

### 四、渐进式披露原则

整个目录遵循四层漏斗（第四层为可选）：

- **第一层 BRIEF.md**：决定用不用、怎么用 → 快速判断是否需要深入
- **第二层 context/current.md**：决定当前该做什么 → 了解状态和任务
- **第三层 context/standards.md**：决定怎么做 → 查看规范和标准
- **第四层 PROJECT_GUIDANCE.md（可选）**：查历史决策和完整细节 → 按需查阅

每一层只回答上一层留下的问题，不要在上一层堆砌下一层的内容。

### 五、首次初始化步骤（Agent 执行顺序）

1. 创建根目录 `BRIEF.md`（含 Guardrail 注释 + **@context-root 契约头** + 项目画像骨架）
2. 创建 `project-context/` 及子目录 `context/`、`references/`、`handoffs/`、`.working/`
3. 创建 `context/current.md`、`context/standards.md`、`changelog.md`（空骨架，**每个均加 @context 契约头**）
4. 创建 `{会议记录目录}/`（如 `B02 会议记录/`）
5. 向用户确认：项目目标、交付物、关键约束、利益相关方；**识别并沿用用户已有的目录编码习惯**（如根目录已存在 `A01`/`B02`/`C01` 等前缀，直接匹配，不另起方案）
6. 回填 BRIEF.md 各板块，写入首条 changelog
7. 如项目规模预计较大，预先创建 `PROJECT_GUIDANCE.md` 骨架

> **跨会话记忆（可选）**：若 Agent 运行环境支持项目外记忆（如 `.workbuddy/memory/`），可额外维护跨会话记忆文件，记录用户偏好与项目长期约定，避免每次会话重新对齐。该文件不纳入 project-context 结构，也不影响本框架的「可迁移性第一约束」——即便没有它，仅靠项目内文件也能接手。

### 六、读取规则

当被要求处理该项目时：

1. 先读 BRIEF.md（含顶部 @context-root 块），获取全局上下文和文件索引
2. 再读 context/current.md（含 @context 块），了解当前状态
3. 根据任务需要，按需读取 context/standards.md 或 references/ 下的具体文件
4. 如需查询历史决策或完整任务记录，按需查阅 PROJECT_GUIDANCE.md（如果有）
5. 不要一次性读取所有文件

### 七、更新规则

| 文件                   | 更新频率      | 更新条件              |
| -------------------- | --------- | ----------------- |
| BRIEF.md 项目画像        | 几乎不变      | 项目目标或约束发生根本变化时才改  |
| BRIEF.md 当前状态        | 每次状态变更时   | 任务完成、新增任务、关键决策    |
| BRIEF.md 文件索引        | 每次文件增删时   | 新增或删除重要文件/目录      |
| context/current.md   | 每次任务推进时   | 任务状态变更、新增决策、发现阻塞点 |
| context/standards.md | 只增不改      | 新增规范或踩坑记录         |
| PROJECT_GUIDANCE.md  | 每次重要事件后   | 详细记录历史决策和完整任务清单   |
| changelog.md         | 每次文件结构变更时 | 新增/修改/删除文件或目录     |

> **⚠️ 注意**：以上是"该改什么"的原则表。具体操作步骤请见下方「更新 SOP」——它告诉 Agent "怎么改、按什么顺序改、哪些文件需要联动"。

---

### 八、更新 SOP（操作指引 —— 强制遵循）

> 当项目状态需要更新时，按对应场景的 SOP 操作，确保相关文件同步更新。
> Agent 收到"更新 brief / 更新项目状态"类指令时，必须先找到对应场景，再按 SOP 操作，**不要只改 BRIEF.md**。

#### 场景一：新会议纪要处理

1. **存入会议纪要**
   
   - 存入项目根目录的 `{会议记录目录}/`（默认 `B02 会议记录/`）
   - 命名规范见下方「文件命名规范」

2. **更新 current.md**（主要工作）
   
   - 读取 `context/current.md`
   - 在"会议纪要索引"中添加一条：日期 + 主题 + ≤5条核心要点 + 文件路径（**不粘贴全文**）
   - 更新"进行中任务"的状态或新增任务
   - 如有新决策，添加到"近期决策"（**记录内容 + 依据/理由 + 日期**）
   - 如有问题已解决，更新"待解决问题"

3. **同步更新 BRIEF.md**
   
   - 读取 `BRIEF.md`
   - 更新"当前状态"的一句话概括
   - 确保与 `current.md` 的"当前状态"一致
   - 如会议纪要需长期引用，在"文件索引"中添加说明

4. **如有重要决策，更新 PROJECT_GUIDANCE.md**
   
   - 读取 `PROJECT_GUIDANCE.md`（如果有）
   - 更新对应章节（时间节点、任务清单、重要决策）
   - 在"更新日志"中添加本次变更记录

#### 场景二：任务状态变更

1. **更新 current.md**（主要工作）
   
   - 在"进行中任务"中找到对应任务
   - 更新任务状态（使用统一状态枚举）
   - 如任务已完成，移到归档区或标记 `已完成`
   - 如有新任务，添加到"进行中任务"

2. **如状态变更影响项目全局，同步更新 BRIEF.md**
   
   - 如任务完成/新增改变了项目整体进度，更新"当前状态"的一句话概括

3. **如任务变更涉及重要决策，更新 PROJECT_GUIDANCE.md**
   
   - 更新任务清单对应条目

#### 场景三：重要决策或需求变更

1. **更新 current.md**
   
   - 在"近期决策"中添加新决策（记录时间、来源、内容、**理由**）
   - 如决策影响进行中任务，同步更新"进行中任务"

2. **同步更新 BRIEF.md**
   
   - 如决策影响项目目标或约束，更新"项目画像"
   - 如决策影响当前状态，更新"当前状态"

3. **更新 PROJECT_GUIDANCE.md**
   
   - 在对应章节记录决策详情（时间、来源、内容、理由）
   - 在"更新日志"中添加本次变更记录

#### 场景四：新增交付物

1. **存入交付物**
   
   - 按产物来源存入对应目录（如 `handoffs/`，或用户既有的交付物目录）
   - 命名规范见下方「文件命名规范」

2. **更新 current.md**
   
   - 如交付物对应某个进行中任务，更新该任务状态为 `已完成`
   - 在"近期成果"中添加交付物引用（如有此章节）

3. **更新 BRIEF.md 文件索引**（如交付物重要）
   
   - 如交付物需要长期引用，在"文件索引"中添加说明

#### 场景五：发现踩坑或新增规范

1. **更新 standards.md**
   
   - 在对应章节添加新规范或踩坑记录
   - **注意**：`standards.md` 只增不改，不要删除已有内容

2. **通知团队**（可选）
   
   - 如踩坑影响其他协作者，在项目群或会议记录中通知

#### 场景六：用户纠正 / Agent 纠错

> 当用户指出 Agent 之前的处理有误（如人名写错、事实偏差、遗漏文件），按此场景修正并留痕。

1. **定位并修正错误文件**
   
   - 直接用 Edit 修正目标文件中的错误内容（如纪要中"唐处"→"汤处"）
   - 如错误已扩散到多个文件，逐一修正，不要只改一处

2. **在相关文件标注修正说明**（视情况）
   
   - 如属关键事实错误，在 current.md 的"近期决策"或 standards.md 的"踩坑记录"中注明，防止再犯
   - 如涉及用户长期偏好（如"规划发展处处长姓汤"），写入跨会话记忆

3. **同步更新 BRIEF.md / PROJECT_GUIDANCE.md**
   
   - 如修正影响项目状态或关键事实，按场景二/三联动更新

4. **记录到 changelog.md**（如涉及文件结构变更）

#### 场景七：项目归档 / 结项

1. **更新 BRIEF.md**
   
   - "当前状态"改为"已结项/已归档"，注明结项日期与最终交付物
   - 将活跃任务清单清空或移至归档区

2. **整理 handoffs/**
   
   - 将最终版交付物归入 `handoffs/`，旧版本可归档或标注"历史版本"

3. **更新 PROJECT_GUIDANCE.md**（如有）
   
   - 标记项目状态为"已结项"，补全最终任务清单与决策时间线

4. **清理 .working/**
   
   - 删除临时脚本与中间产物，保持目录整洁

---

### 九、会话恢复协议与增量落盘纪律（跨平台接手保险）

> **平台约束（出发点）**：当前主流 Agent 平台（WorkBuddy / Trae / Qoderwork 等）在对话中断后**无法自主动作**——Agent 进程在中断时挂起，只有用户重启会话才能唤醒，它不可能在"没人唤醒"的情况下自己改写 BRIEF。因此，"会话异常结束时自动全量同步 BRIEF"这类写法在本框架下**不可实现**，必须改为"恢复时触发"的设计。

#### 9.1 会话恢复协议（触发点在"恢复时"）

任何 Agent **重新接手**本项目时（无论新会话、跨平台切换、还是中断后恢复），第一步**必须**：

1. 读取 `BRIEF.md`（含 @context-root 块）与 `context/current.md`（含 @context 块）；
2. 比对两者的最后更新时间，确认状态一致；
3. 若发现状态不一致、或存在未闭合的 in-flight 任务，先 **reconcile**（必要时向用户确认），再开始新工作；
4. 不假设"上次会话已正常收尾"——以文件内容为唯一权威。

> 该协议已通过实测：全新子 Agent（不带历史记忆、禁读平台记忆目录）仅依 BRIEF + @context 块即可正确接手并给出接力顺序，证明项目已自描述、可迁移。

#### 9.2 增量落盘纪律（根因治理）

- `BRIEF.md` / `context/current.md` 是项目**唯一权威真相源**；
- 任何任务步骤完成，**立即落盘更新对应文件**，不等会话结束统一补；
- 对话上下文（transcript）本身**不具权威性**——真正会丢的只有"还没写进文件、只活在对话里"的那部分；
- 只要每步即落盘，中断最坏只丢最后一个 in-flight 步骤，重启后顺着 @context 的 `downstream` 接力关系接着走即可。这比"自动全量同步"更可靠，因为后者还假设 Agent 活着。

#### 9.3 定时自动化巡检（可选，非必需）

WorkBuddy 等平台的 automation 按 schedule 独立跑、不依赖当前对话。可设周期性检查"BRIEF 与 current 是否一致"，但它**不知道"刚中断"**，只是机械巡检，价值有限还可能制造噪音——本框架**不将其作为主方案**，仅作锦上添花，由用户按需自定。

---

### 十、Agent 工作规范（强制执行）

> 以下规则针对 AI Agent，防止其污染项目结构。

1. **根目录保护**
   
   - 项目根目录只能有：BRIEF.md、project-context/、{会议记录目录}/、用户明确认可的目录
   - Agent **不得**在根目录创建临时脚本、测试文件、中间产物

2. **临时文件管理**
   
   - Agent 自生成文件（临时脚本、测试输出、中间缓存）一律放入 `project-context/.working/`
   - 任务完成后主动清理临时文件，`.working/` 不纳入版本管理
   - 如有需要保留的中间产物，先询问用户再决定存放位置（沿用用户既有的过程文档目录，如 `A01 中间文档/` 之类，不新建编码体系）

3. **文件创建原则**
   
   - 只在用户明确要求或项目 SOP 规定时创建新文件
   - 优先编辑已有文件而非新建；current.md / PROJECT_GUIDANCE.md 采用增量编辑，**避免整篇重写**

4. **增量更新纪律**
   
   - 状态更新用 Edit 定向修改对应段落，不要用 Write 覆盖整个文件
   - 整篇重写仅在文件结构重大调整或用户明确要求时进行

---

### 十一、文件命名规范

> 统一日期格式，避免不同协作者产生命名混乱。分隔符可用 `-` 或 `_`，但项目内须统一。

| 文件类型 | 命名格式                    | 示例                      |
| ---- | ----------------------- | ----------------------- |
| 会议纪要 | `YYYYMMDD-会议主题-纪要.md`   | `20260624-内部分工会议-纪要.md` |
| 会议原文 | `YYYYMMDD-会议主题-原文.md`   | `20260624-内部分工会议-原文.md` |
| 交付物  | `YYYYMMDD-内容描述-版本号.扩展名` | `20260630-建设方案-v3.docx` |
| 参考资料 | `YYYYMMDD-内容描述.扩展名`     | `20260515-某厅来函.pdf`     |

> 日期格式统一为 `YYYYMMDD`（8位数字），便于按时间排序和检索。会议主题尽量用短词，避免过长文件名。

---

### 十二、工具与脚本依赖的跨电脑可迁移性规范（v1.1 新增）

> **出发点**：BRF 的「可迁移性第一约束」不只要求*文档*能跨平台读，还要求*项目跑起来所需的工具、脚本、配置*也能跟着项目走。否则换个电脑、换个同步环境，文档在而工具链断，接手者照样寸步难行。

#### 12.1 通用原则（BRF 硬约束）

1. **依赖随项目携带**：项目运行所需的工具、脚本、配置，一律纳入项目文件夹，不依赖「某台电脑已装好」或「某平台全局已配好」。
2. **路径禁止写死用户名**：脚本内涉及路径时，禁止硬编码 `C:\Users\Ryan\…` 之类绝对用户名路径；改用相对路径，或用 `os.path.expanduser("~")` 等运行时展开。
3. **不建平台专属目录**：BRF 方法论不绑定任何平台，因此项目内**不得**创建 `.workbuddy/skills/`、`.obsidian/` 等平台私有隐藏目录来承载通用能力。如需存放项目自带的工具脚本，统一走 `project-context/tools/`（纯文件、全平台可读、随项目同步）。

#### 12.2 跨电脑部署模式：非隐藏携带 + 本地 regenerate

当项目通过 NAS / 网盘等同步工具在多台电脑间流转时，多数同步工具**默认排除隐藏文件夹**（`.workbuddy/`、`.obsidian/` 等不会同步）。这带来一个确定性坑：

> 若把能力文件（skill、snippet、配置）塞进隐藏目录，换电脑后这些文件根本不会同步过来，新机器上的项目成了「缺胳膊少腿」的残本。

实测有效的应对模式：

- **能力文件非隐藏携带**：把项目真正依赖的能力（如抽取脚本、CSS snippet、工具脚本）以**非隐藏**形式放在项目内（如 `project-context/tools/` 或随能力自带的非隐藏目录），确保同步工具能带上它们；
- **隐藏落点本地 regenerate**：平台专属的隐藏落点（如某平台要求把 skill 放到 `.workbuddy/skills/` 下才能识别）**不要靠同步搬运**，而是提供一个项目内的激活脚本（如 `deploy.py` 或 `ensure_xxx()` 函数），在新机器首次运行时**本地重新生成**这些隐藏目标；
- **源随能力自带**：激活脚本所引用的「源」（模板、片段原文）也要随项目携带，激活脚本只做「复制 / 生成到平台专属位置」这一件事。

#### 12.3 依赖声明式管理 / 跨平台部署清单

每个使用 BRF 的项目，应在 `standards.md`（或独立清单文件）维护一张「跨平台部署清单」，区分两类：

| 类别 | 含义 | 处理方式 |
|------|------|----------|
| 已随项目携带 | 工具 / 脚本 / 配置已纳入项目文件夹，拷走即用 | 新 Agent 直接读，无需额外动作 |
| 需新环境安装 | 运行时依赖（社区技能、Python venv、平台插件等）未在项目内 | 标注**安装方式**（命令 / 链接），新 Agent 接手逐项核对、按需安装 |

> 该清单是新 Agent 接手时的「体检表」：先核「已携带」项是否齐全，再处理「需安装」项，避免漏装导致项目跑不起来。

### 十三、项目自描述增强：身份速览块 + 接手自检清单（v1.2 新增）

> 来源：从标准化 skill 的「元信息自描述」（frontmatter：name / description / use-when）与「打包前实测」两处实践中抽象出的通用方法。BRF 自身不套 skill 外壳（定位见项目空间 standards.md 第七章），但吸收其标准化思想——让任何项目在接手者眼中「一眼可懂、接手有章」。

#### 13.1 项目身份速览块（30 秒接手卡）

每个 BRF 项目的 `BRIEF.md` 顶部，应设一个纯 Markdown 的「项目身份速览块」（不依赖 YAML / 隐藏目录，任何平台可读、根目录可见），至少回答四项：

| 字段 | 必答问题 | 作用 |
|------|---------|------|
| 是什么 | 这个项目 / 这套上下文的本质是什么 | 接手者 30 秒建立心智模型，避免误判定位 |
| 何时深入读 | 什么情况下才需要展开读项目空间细节 | 区分「套用框架」与「维护框架」两类接手者，省去无关阅读 |
| 接手最小动作 | 动手前最低限度的必读 + 必做 | 防止「没读就改」导致上下文漂移 |
| 前置依赖 | 运行 / 发布所需的前置条件 | 接手者提前确认环境，避免卡在认证 / 工具链 |

> 要点：速览块对标 skill 的 frontmatter，但**刻意不用 YAML**——BRF 的第一约束是可迁移性，YAML 与平台隐藏目录会破坏「纯 Markdown + 根目录可见」。机器可读元数据交给 HTML 注释 `@context-root` 块承载，人 / Agent 可读摘要交给纯 Markdown 速览块，二者互补。

#### 13.2 接手自检清单（对标 skill 打包前实测）

在「会话恢复协议」（第九章）处，应附一张「接手自检清单」，新 Agent 接手时**逐项打勾再动手**，对标 skill「发布前实测通过」的纪律：

- [ ] 已读 `BRIEF.md`（含 @context-root 块）+ 当前状态文件（含 @context 块），两者最后更新时间一致
- [ ] 已判定本次指令属哪一轨：项目空间维护，还是公开规范发版（见双轨声明）
- [ ] 已核对当前状态与进行中任务，无未闭合 in-flight 任务（或已 reconcile）
- [ ] 如涉发布：已确认认证 / 范围 / 署名三条红线
- [ ] 已明确本次改动的 downstream 接力顺序（改完下一步动哪个文件）

> 这张清单是「会话恢复协议」的可执行化：把"恢复时该做什么"从原则变成勾选动作，降低新 Agent 漏步概率；它也是 new-Agent-takeover 的验收标准——能逐条打勾，说明项目自描述达标。

---

### 十四、留痕与决策生命周期（四态模型）

> **出发点**：项目真正危险的不是文档暂时不用，而是需要回顾时发现没有证据——无法解释为什么这样设计，也无法判断旧问题是否重新出现。先留痕，才有可能系统分析。开发过程中的需求讨论、方案取舍、错误、修复和评测结果，都应被结构化记录，而非只活在对话里。

#### 14.1 四态生命周期（每条目处于且仅处于一态）

每个决策 / 问题 / 方案 / 评测条目，应明确归属于以下四态之一，并在状态变更时显式迁移：

| 态 | 含义 | 进入条件 | 典型落地文件 |
|----|------|---------|------------|
| **Open** | 仍在讨论的问题、假设、风险、待验证事项 | 提出但尚未拍板或验证 | `current.md`「开放问题 / 假设 / 风险」区块 |
| **Decisions** | 已做出的决策、依据、负责人、影响范围 | 已拍板（含依据/理由） | `current.md`「近期决策」+ `PROJECT_GUIDANCE.md` |
| **Resolved** | 已解决的问题、修复方式、对应回归用例 | 问题闭环且验证通过 | `current.md`「待解决问题」清除 + `standards.md`「踩坑记录」 |
| **Archive** | 已失效但仍需追溯的方案与评测（退役 ≠ 删除） | 方案被取代 / 功能下线 / 评测未采纳 | git tag / `handoffs/` / 迭代「排除项」 |

> 关键纪律：一条目同一时刻只属于一态。Open → Decisions → Resolved/Archive 是正常流转；状态迁移本身也要落盘，不留"幽灵条目"。

#### 14.2 Open 态：把"还没想清 / 还没验证"显式化

Open 态是多数项目最薄弱的一环。它刻意与「进行中任务」解耦：

- **任务表**管"做什么"（已明确要执行的工作）；
- **开放问题表**管"还没想清什么 / 还没验证什么"（假设、风险、待决疑问）。

`current.md` 应设独立区块记录：假设（Assumption）、风险（Risk）、待验证事项（To-verify）。这些条目不被催着"赶紧定"，但必须被看见——复盘时它们是解释"为什么后来改了方向"的关键证据。

#### 14.3 Decisions 态：决策 + 依据 + 负责人 + 影响范围

BRF 早已强制"决策须记依据/理由"（见第三章 `current.md` 近期决策）。v1.3 进一步要求每条决策至少含四字段：**内容 + 依据/理由 + 负责人 + 影响范围**。

- 负责人：谁拍的板 / 谁负责落地；
- 影响范围：波及哪些文件、模块、协作者或下游项目。

这四项齐全，接手者不只看结论，更能判断"这条决策现在还成立吗、动它会影响谁"。

#### 14.4 Archive 态：退役不等于删除

被取代的方案、下线的功能、未采纳的评测，移入 Archive 并保留"为什么退役"——这是追溯的底线。

- 现有基础设施已天然承接 Archive 态：`git tag` 锚定历史版本、`handoffs/` 存历史交付物、公开规范迭代时的「排除项」记录"进不进规范的理由"；
- Archive 的条目**不删除**，只标注退役原因与生效时点，使日后"旧方案是否该复活""旧问题是否重现"有据可查。

#### 14.5 原始证据 vs 结构化文档：分工而非替代

平台（Codex / Claude Code / WorkBuddy 本地等）通常会在本地保存会话记录。它们可以作为**原始证据**——让另一个 Agent 只分析用户输入、决策变化或失败路径。但聊天记录**不能替代**结构化文档：文档读取更快、边界更清晰，也更适合团队协作与版本管理（与第九章"transcript 不具权威性"一致）。

> 实践要点：原始会话记录是"备查的原始底稿"，四态文档是"对外可协作的权威真相"。两者并行——既保留原始证据，又不被原始证据绑架。

---

### 十五、四态巡检清单与活跃上下文轻量化（v1.4 新增）

> **来源**：v1.3 第十四章把 Open/Decisions/Resolved/Archive 四态生命周期显式化后，BRF 项目空间在 2026-08-14 自身按四态模型重构 `current.md`（四态视图 + 历史史料归档）。这次"框架先践行自己提倡的留痕体系、再反哺通用方法"的实践，沉淀出两条可通用的收尾纪律——它们经双轨筛选（领域无关 / 不含本地运维细节 / 可被他人套用）进入本规范。

#### 15.1 四态巡检清单（迭代收尾必做）

每次迭代 / 项目阶段收尾时，按 Open→Decisions→Resolved→Archive 逐项自检。**四态均闭合，该轮迭代才算完成**；任一态有未闭合项，要么本轮解决，要么显式继承到下一轮（留 Open 或标注 Archive 原因），不得"静默吞下"。

| 态 | 自检问题 | 闭合判据 |
|----|---------|---------|
| **Open** | 还有未闭合的假设 / 风险 / 待验证项吗？ | 全部已处理：或移 Decisions（已拍板）、或移 Resolved（已验证）、或明确继承下一轮并留痕 |
| **Decisions** | 本轮所有重要决策都记齐四字段了吗？ | 内容 + 依据/理由 + 负责人 + 影响范围 齐全 |
| **Resolved** | 已解决的问题都有记录且可回归验证吗？ | 修复方式 + 回归用例 / 可验证证据 落盘 |
| **Archive** | 退役 / 取代的旧方案保留"为什么退役"了吗？ | 退役原因 + 生效时点 标注，未删除 |

> 要点：巡检清单是 v1.3 四态模型的"操作化收尾"。它把"先留痕再分析"从原则变成迭代必做的勾选动作，防止"发了版但 Open 态还挂着一堆没说清的事"这类半拉子收尾。

#### 15.2 活跃上下文轻量化（渐进式披露实操）

四态归档实践反哺出第二条纪律：活跃文件只留"当前态 + 指针"，历史史料归档保留原文不删。

- **活跃文件（如 `current.md`）**：只保留当前态的 Open/Decisions/Resolved/Archive + 指针（"详情见 `PROJECT_GUIDANCE.md` 第 X 章"），不堆砌历史全文；
- **历史史料归档**：搭建时间线、迭代候选全文、历史待办等迁入 `PROJECT_GUIDANCE.md`（或 `handoffs/`），原文保留不删，仅从活跃视图移除；
- **效果**：活跃文件保持轻量（新 Agent 接手聚焦当下），史料仍可追溯（符合 v1.3 Archive 态"退役≠删除"）。

> 与 v1.3 的衔接：轻量化不是"丢弃历史"，而是把历史从"活跃视线"移到"归档层"——既满足渐进式披露（第四层按需查阅），又守住四态追溯底线。

#### 15.3 与发布流程的衔接（发布闸门）

四态巡检清单是迭代收尾的"发布闸门"：写公开规范正文前，先跑一遍 15.1 巡检——Open 态无遗留、Decisions 四字段齐、Resolved/Archive 可追溯，再走双轨流程第四步（写 `handoffs/` 正文 → 升 README → 发 GitHub 打 tag）。这把 v1.3 的"留痕"与 v1.4 的"收尾自检"串成闭环，也是本规范自身 v1.4 发版的实操印证。

---

### 十六、四态就绪门禁与平台入口适配器（v1.5 新增）

> **来源**：2026-08-17 GitHub 竞品研究（cursor-memory-bank 3,055★ / my-claude-code-setup 2,585★ / roo-code-memory-bank 1,677★ / RooFlow 1,232★ / memory-bank-mcp 916★ / BMAD-METHOD 51,966★ / OpenSpec 65,078★ 等）。BRF 项目空间在 v1.4 落地「四态巡检清单」后，对照 Memory Bank 系"IDE 事件自动更新 + UMB 手动兜底"与 AGENTS.md/CLAUDE.md 平台入口约定，提炼出两项可通用的增强——均经双轨筛选（领域无关 / 不含本地运维细节 / 可被他人套用）进入本规范。

#### 16.1 四态就绪门禁（机器可验证交接）

v1.4 的「四态巡检清单」是人工勾选。v1.5 将其升级为**机器可验证的交接就绪门禁**：一个轻量校验脚本，在落盘 / 收尾时自动核对 Open/Decisions/Resolved/Archive 四态闭合度，输出可读"就绪报告"，把"人记得勾"变成"机器替你查"。

- **形态**：放在 `project-context/tools/` 的纯文件脚本（Shell / Python / PowerShell 均可），全平台可读、随项目携带（参见 v1.1 第十二章）；不绑定任何 IDE，也不强制运行时环境——没有就人工跑，有就一键跑。
- **门禁逻辑**（BRF 只规定"查什么 + 输出什么"，不规定语言）：解析 `current.md` 的四态标记，逐项判定：

  | 态 | 门禁检查项 | 闭合判据 |
  |----|-----------|---------|
  | **Open** | 是否还有未闭合的假设 / 风险 / 待验证项？ | 全部已处理（移 Decisions / Resolved，或显式继承下一轮并留痕） |
  | **Decisions** | 本轮决策是否四字段齐全？ | 内容 + 依据/理由 + 负责人 + 影响范围 齐全 |
  | **Resolved** | 已解决项是否有修复方式 + 可验证证据？ | 修复方式 + 回归用例 / 证据 落盘 |
  | **Archive** | 退役项是否保留"为什么退役"？ | 退役原因 + 生效时点 标注，未删除 |

- **输出**：一份"就绪报告"——哪些态已闭合、哪些态有缺口、缺口定位到行，供发布前（呼应 15.3 发布闸门）或任意检查点使用。
- **与发布流程衔接**：15.3 的发布闸门从"人工跑巡检"升级为"人工或脚本跑门禁"——门禁输出"四态已闭合"是发布前置条件之一。

> 要点：门禁是 v1.4 巡检清单的"操作化再升级"，不是新依赖。BRF 不规定脚本语言，只规定"门禁应检查什么 + 输出什么"；具体实现由项目自选，参考 `tools/publish.ps1` 的纯文件 + 全平台可读模式即可。

#### 16.2 平台入口适配器（BRIEF⇄AGENTS.md/CLAUDE.md 双向映射）

BRF 的单一入口是 BRIEF（纯 Markdown、平台无关）。但主流编码 Agent 平台（Cursor / Claude Code / Roo Code 等）在会话开始时会自动读取 `AGENTS.md` / `CLAUDE.md` 作为项目入口。BRF 项目若只维护 BRIEF，就错失了这些平台的"自动接手"通道。平台入口适配器解决这个问题——**不替换 BRIEF 权威，而是把 BRIEF 适配到各平台入口**。

- **单一真相源不变**：BRIEF 仍是唯一权威入口；`AGENTS.md` / `CLAUDE.md` 是**派生视图**（由 BRIEF 生成，不是另起炉灶）。
- **双向映射规范**（纯 Markdown，可剥离 BRF 内部上下文独立套用）：

  | BRIEF 区块 | 映射到平台入口 | 说明 |
  |-----------|--------------|------|
  | 项目身份速览块（v1.2 13.1） | 平台入口文件顶部"项目概述" | 30 秒接手卡原样下沉 |
  | 文件组织 + 文件索引（BRIEF 二/四） | "项目结构 / 关键文件"段 | 告诉平台 Agent 先看哪几个文件 |
  | 会话恢复协议 + 接手自检清单（第九/十三章） | "工作规则 / 接手步骤"段 | 让平台 Agent 也走恢复协议，不假设自动同步 |
  | 当前状态 + 近期决策 | "进行中 / 最近决策"段（可选，定期刷新） | 平台内 Agent 也能看到当下进度 |

- **一键产出 / 回收**：项目内提供一段脚本或文档化步骤，从 BRIEF 生成 `AGENTS.md` / `CLAUDE.md`（正向），并把平台内手动改动反向合并回 BRIEF（反向，可选）——收回时以 BRIEF 为准，避免双源漂移。
- **不违反去平台绑定红线**：适配器是"映射层"而非"替代层"——核心仍是纯 Markdown BRIEF；映射规则本身是平台无关的纯文本，只描述"如何对接某平台入口"。它解决的是"BRF 项目也能吃上平台自动接手红利"的痛点，而非把 BRF 绑死到某平台。

> 要点：`AGENTS.md` / `CLAUDE.md` 是"平台侧的 BRIEF 镜像"，不是第二个真相源。任何时候以 BRIEF 为准，适配器只负责翻译与同步。

#### 16.3 刻意排除：全量 IDE 自动同步钩子

竞品（Memory Bank 系）普遍用"IDE 事件自动更新 + UMB 手动兜底"做上下文接力。BRF **不把"实时监听会话事件自动改写 BRIEF"** 纳入公开规范——原因：① 监听平台会话事件需要平台专属钩子，违反 BRF"去平台绑定"第一约束；② 单平台钩子可行但把 BRF 绑死到该平台，违背元框架定位。BRF 已有"会话恢复协议 + 增量落盘 + 接手自检清单"三件套对抗中断 / 丢失，全量自动同步是 nice-to-have 而非 must。该方向留作**项目空间实验项**，不进公开规范；若未来要做，优先做成"平台无关的中转协议"而非"平台专属钩子"。
